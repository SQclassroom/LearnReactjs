import React from 'react'


const List1 = ["one" , "two" , "three"]
const Found = List1.find(element => element === "three")

if(Found === "one"){
  
}

const Footer = () => {
  return (
    <div>
      
    </div>
  )
}

export default Footer